# name: Deploy Staging to Docker Hub

# on:
#   push:
#     branches:
#       - linda 

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v3

#       - name: Log in to Docker Hub
#         uses: docker/login-action@v2
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Build and Push App Docker Image
#         run: |
#           docker build -f Docker/Dockerfile/app.Dockerfile -t 3b052lindasantika/assessment-app-kota201 .
#           docker push 3b052lindasantika/assessment-app-kota201

#       - name: Build and Push Web Server Docker Image
#         run: |
#           docker build -f Docker/Dockerfile/web_server.Dockerfile -t 3b052lindasantika/assessment-app-kota201-web_server:dev .
#           docker push 3b052lindasantika/assessment-app-kota201-web_server